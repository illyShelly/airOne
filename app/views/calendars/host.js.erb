// get all events from controller
reservations = <%= raw @events.to_json %>
// needs to update -> if disable future moths (in 2m) it is in DB but after refresh not visible
days = <%= raw @days.to_json %>

$('#calendar').fullCalendar('removeEvents');
// add above evens
$('#calendar').fullCalendar('addEventSource', showReservations(reservations));
$('#calendar').fullCalendar('rerenderEvents');
